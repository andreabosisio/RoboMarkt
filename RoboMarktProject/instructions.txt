1. Install the requirements (AMPL API) by typing:

python -m pip install -r requirements.txt

3. Run the python project by typing:

python main.py ampl_folder input_data_folder

with ampl_folder the path of the folder where AMPL is installed and input_data_folder the path folder of the input data.